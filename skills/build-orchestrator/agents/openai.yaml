interface:
  display_name: "Build Orchestrator"
  short_description: "Plan with you, delegate builds, judge results"
  default_prompt: "Use $build-orchestrator to plan this work with me, delegate the building to our local builder, and bring me decisions backed by evidence."
policy:
  allow_implicit_invocation: true
